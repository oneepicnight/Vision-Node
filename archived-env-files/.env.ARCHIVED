# Vision Node Constellation Configuration v1.0.0 (MAINNET)
# Node Mode: Constellation (Regular Node)

VISION_GUARDIAN_MODE=false
BEACON_MODE=passive
VISION_PORT=7072
VISION_ANCHOR_SEEDS=35.151.236.81,16.163.123.221,69.173.206.211,69.173.207.135,74.125.212.204,75.128.156.69,98.97.137.74,182.106.66.15
VISION_HOST=0.0.0.0
RUST_LOG=info

# ============================================
# 🔍 P2P DEBUG MODE (for connection issues)
# ============================================
# If your node can't connect to other v1.0.0 nodes:
# 1. Uncomment the line below to bypass handshake validation
# 2. Restart the node
# 3. Check logs for warnings showing which fields mismatch
# 4. Visit http://localhost:7070/api/p2p/debug on both nodes to compare
#
# VISION_P2P_DEBUG_ALLOW_ALL=1
#
# This will allow connections from peers with:
# - Mismatched bootstrap prefix (different network build)
# - Out-of-range protocol versions
# - Old node versions
#
# WARNING: Only use for debugging! Turn off for production.
# ============================================

# 🌌 PURE SWARM MODE (Guardian-less Operation) - DEFAULT
# Network operates completely decentralized without guardian dependency
VISION_PURE_SWARM_MODE=true
VISION_BEACON_BOOTSTRAP=false
VISION_GUARDIAN_RELAY=false

# Swarm Intelligence Settings
# Full mesh networking - everyone connects to everyone
VISION_MAX_PEERS=1000
VISION_MIN_HEALTHY_CONNECTIONS=5
VISION_SWARM_INTELLIGENCE=true
# VISION_HEALING_INTERVAL_SECS=60
# VISION_RETRY_INTERVAL_SECS=30

# ⛏️ MINING GATE SYSTEM - Prevents premature mining
# Ensures network readiness before block production begins
VISION_MIN_PEERS_FOR_MINING=2
# VISION_MINING_READINESS_TIMEOUT_SECS=60  (optional - no timeout by default)

# 🌐 P2P REACHABILITY & ADVERTISEMENT (Phase 10)
# Optional: Override external IP/port for P2P advertisement
# If not set, will auto-detect via ipify.org + STUN fallback
# VISION_PUBLIC_IP=<your_external_ip>
# VISION_PUBLIC_PORT=7072  (defaults to VISION_PORT if not set)

# Beacon endpoint (only used if VISION_PURE_SWARM_MODE=false)
BEACON_ENDPOINT=https://visionworld.tech

# 🌐 HTTP CONTROL PLANE ANCHORS (7070) - v2.7.0+
# Comma-separated list of anchor IPs (ports auto-added as :7070)
# Falls back to genesis seed IPs if not set
VISION_ANCHOR_SEEDS=35.151.236.81,16.163.123.221,69.173.206.211,69.173.207.135,74.125.212.204,75.128.156.69,98.97.137.74,182.106.66.15

# Asset directories (relative to executable)
VISION_PUBLIC_DIR=public
VISION_WALLET_DIR=wallet/dist
